import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Nota: El redirect www → no-www está configurado en vercel.json
  // para evitar conflictos y loops de redirección
};

export default nextConfig;
